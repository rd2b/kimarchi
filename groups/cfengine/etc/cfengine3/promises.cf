body common control
{
  bundlesequence  => { "update", "init" };
  inputs          => { "init.cf" };

}
#######################################################
bundle agent update
{
files:
  any::
     "/tmp/test-cfengine3"

    comment => "Exemple creation fichier par cfengine3",
    create  => "true";
}
