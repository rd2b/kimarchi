body common control
{
  bundlesequence  => { "update", "init", "pkg", "files" };
  inputs          => { "init.cf", "pkg.cf", "cfengine_stdlib.cf", "files.cf" };

}
#######################################################
bundle agent update
{
files:
  any::
     "/tmp/test-cfengine3"

    comment => "Exemple creation fichier par cfengine3",
    create  => "true";
}
