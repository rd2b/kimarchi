body common control
{
  bundlesequence  => { "update",
                       "init", 
                       "pkg", 
                       "files", 
                       "default" };
  inputs          => { "update.cf", 
                       "init.cf", 
                       "pkg.cf", "cfengine_stdlib.cf", 
                       "files.cf", 
                       "default.cf", "cf-serverd.cf" };

}

body agent control {
# Do not rely on DNS
    skipidentify => "true";
}

