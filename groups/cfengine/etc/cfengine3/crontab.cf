bundle agent crontabs
{
vars:
    cfmaster::
        "cfrepos" => "/data/cf-repos/.git";

commands:
    cfmaster.Q1::
        "/usr/bin/git"
            args => "pull",
            chdir => "$(cfrepos)";

}
