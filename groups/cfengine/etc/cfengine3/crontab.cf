bundle agent crontabs
{
vars:
    cfmaster::
        "cfrepos" string => "/data/cf-repos/.git";

commands:
    cfmaster.(Q1|Q2|Q3|Q4)::
        "/usr/bin/git"
            args => "pull",
            contain => go_to_dir("$(cfrepos)");

}

body contain go_to_dir(d)
{
    chdir => "$(d)";
}
